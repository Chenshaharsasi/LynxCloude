# macOS
.DS_Store

# Claude Code local settings (per-machine)
.claude/settings.local.json

# Environment variables (secrets — never commit)
.env
.env.local
.env.*.local

# Obsidian (per-machine workspace state — not project knowledge)
.obsidian/

# Studio data — contains member PII and financial info; never commit
data/members.csv
data/budget.csv
data/revenue.csv
data/*.private.csv
